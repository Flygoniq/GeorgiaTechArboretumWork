import { chooseAttribute } from 'ember-two/helpers/choose-attribute';
import { module, test } from 'qunit';

module('Unit | Helper | choose attribute');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = chooseAttribute([42]);
  assert.ok(result);
});
